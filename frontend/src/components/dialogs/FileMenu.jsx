import { Menu } from "@mui/material";
import React from "react";

const FileMenu = ({ anchor }) => {
  return (
    <>
      <Menu anchorEl={anchor}> sad</Menu>
    </>
  );
};

export default FileMenu;
